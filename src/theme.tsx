import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#9e9e9e"
    },
    background: {
      default: "#222"
    },
    text: {
      primary: "#e1e1e1"
    }
  },
})

export default Theme